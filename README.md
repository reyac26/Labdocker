
# Labdocker

# Labdocker - Our Application (Getting Started)

This repo contains the **Dockerfile** created in the Docker "Getting Started / Our Application" tutorial.

## Prerequisites
- Docker installed 
- (Linux users) you may need to run Docker commands with 'sudo'

## Build the image
From the folder that contains the Dockerfile:

'''bash 
sudo docker build -t getting-started .
